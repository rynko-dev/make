{
  "name": "listPdfTemplates",
  "label": "List PDF Templates",
  "description": "Fetches PDF templates for dynamic dropdowns",
  "connection": "oauth2",
  "api": {
    "url": "{{base.baseUrl}}/templates",
    "method": "GET",
    "headers": "{{common.headers}}",
    "qs": {
      "format": "pdf",
      "limit": 100
    },
    "response": {
      "iterate": "{{body.data}}",
      "output": {
        "id": "{{item.id}}",
        "name": "{{item.name}}",
        "description": "{{item.description}}"
      }
    }
  }
}
