{
  "name": "listTemplates",
  "label": "List Templates",
  "description": "Fetches templates for dynamic dropdowns",
  "connection": "oauth2",
  "api": {
    "url": "{{base.baseUrl}}/templates",
    "method": "GET",
    "headers": "{{common.headers}}",
    "qs": {
      "limit": 100
    },
    "response": {
      "iterate": "{{body.data}}",
      "output": {
        "id": "{{item.id}}",
        "name": "{{item.name}}",
        "description": "{{item.description}}",
        "format": "{{item.format}}"
      }
    }
  }
}
